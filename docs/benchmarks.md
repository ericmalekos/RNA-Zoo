# Benchmarks

This page is auto-generated by `.github/workflows/benchmark.yml` — a weekly workflow that runs `-profile test,docker,cpu` with `-with-trace` on a GitHub Actions `ubuntu-latest` runner and summarizes the per-process wall-clock, peak RSS, and peak virtual memory.

The first run will populate this file with a Markdown table.
